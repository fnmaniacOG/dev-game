[workspace]
members = [
  "programs/token_factory",
  "programs/liquidity_lock",
  "programs/game_state",
  "programs/tournament",
  # "programs/vrf_consumer",  # disabled: switchboard conflict
  "programs/liquidity_matching",
  "programs/governance",
  "programs/clockwork_automation",
  "programs/referral",
]
resolver = "2"

[profile.release]
overflow-checks = true
lto = "thin"
codegen-units = 1

[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1
